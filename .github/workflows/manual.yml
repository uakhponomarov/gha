name: Manual test

on: 
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  first:
    runs-on: [self-hosted, docker]
    steps:
      - name: Checkout
        uses: actions/checkout@v3.5.3
      - name: Hello
        run: echo Hello; sleep 100; ls -l
  second:
    needs: first
    runs-on: [self-hosted, docker]
    steps:
      - name: Hello
        run: echo Hello second; sleep 100; ls -l
  third:
    needs: first
    runs-on: [self-hosted, docker]
    steps:
      - name: Hello
        run: echo Hello third; sleep 100; ls -l       
